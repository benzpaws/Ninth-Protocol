---
// Stub. Real §IV Contact port comes fourth in the section pass.
---

<section class="stub-section stub-section--contact" id="contact">
  <span class="stub-section__label">§IV · Contact · [Contact + InquiryForm]</span>
</section>

<style>
  .stub-section {
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 100px 36px 60px;
  }
  .stub-section--contact {
    background: linear-gradient(180deg, var(--color-indigo-deep) 0%, var(--color-indigo-ink) 100%);
    color: var(--color-gold);
  }
  .stub-section__label {
    font-family: var(--font-mono);
    font-size: 14px;
    letter-spacing: 0.32em;
    text-transform: uppercase;
  }
</style>
