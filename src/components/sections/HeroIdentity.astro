---
// Stub. Real §I Identity port comes first in the section pass.
---

<section class="stub-section stub-section--hero" id="hero">
  <span class="stub-section__label">§I · Identity · [HeroIdentity]</span>
</section>

<style>
  .stub-section {
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 100px 36px 60px;
  }
  .stub-section--hero {
    background: var(--color-indigo-night);
    color: var(--color-gold);
  }
  .stub-section__label {
    font-family: var(--font-mono);
    font-size: 14px;
    letter-spacing: 0.32em;
    text-transform: uppercase;
  }
</style>
