---
// Stub. Real §II The Model port comes second in the section pass.
---

<section class="stub-section stub-section--model" id="model">
  <span class="stub-section__label">§II · The Model · [Model + FeePanel]</span>
</section>

<style>
  .stub-section {
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 100px 36px 60px;
  }
  .stub-section--model {
    background: var(--color-paper);
    color: var(--color-ink);
  }
  .stub-section__label {
    font-family: var(--font-mono);
    font-size: 14px;
    letter-spacing: 0.32em;
    text-transform: uppercase;
  }
</style>
