---
// Stub. Real implementation comes in the chrome pass after sections are ported.
---

<div class="stub stub--drawer" data-stub="MobileDrawer" hidden>[MobileDrawer]</div>

<style>
  .stub--drawer {
    position: fixed;
    inset: 0;
    z-index: 60;
    padding: 32px;
    background: var(--color-indigo-night);
    color: var(--color-gold);
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
  }
</style>
