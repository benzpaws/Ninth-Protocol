---
// Stub. Real implementation comes in the chrome pass after sections are ported.
---

<div class="stub stub--topbar" data-stub="TopBar">[TopBar]</div>

<style>
  .stub--topbar {
    position: sticky;
    top: 0;
    z-index: 50;
    padding: 12px 20px;
    background: rgba(14, 11, 34, 0.84);
    border-bottom: 1px dashed var(--color-gold);
    color: var(--color-gold);
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
  }
</style>
